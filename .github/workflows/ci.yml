name: Build and Containerize

on:
  push:
    branches: [ "main" ]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a greeting
        run: echo "Code pushed to main. Starting CI process..."